{
  "name": "dan-ide",
  "version": "0.1.0",
  "description": "Local agentic development environment for managing AI coding sessions",
  "main": "src/main/main.js",
  "scripts": {
    "start": "npm run sign && electron .",
    "dev": "npm run sign && electron . --dev",
    "test": "echo \"No tests configured\" && exit 0",
    "sign": "codesign --force --deep --sign - --entitlements entitlements.plist node_modules/electron/dist/Electron.app 2>/dev/null || true"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-web-links": "^0.11.0",
    "@xterm/xterm": "^5.5.0",
    "electron": "^33.0.0",
    "monaco-editor": "^0.55.1",
    "node-pty": "^1.0.0",
    "ws": "^8.20.1"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.0.4"
  }
}
